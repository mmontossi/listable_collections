mysql: &mysql
  adapter: <%= 'jdbc' if RUBY_ENGINE == 'jruby' %>mysql

postgres: &postgres
  adapter: <%= 'jdbc' if RUBY_ENGINE == 'jruby' %>postgresql

default: &default
  <<: *<%= ENV['DB'] %>

development:
  <<: *default
  database: travis

test:
  <<: *default
  database: travis
